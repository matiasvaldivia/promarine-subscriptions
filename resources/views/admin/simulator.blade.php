<x-admin-shell><span class="badge mock">SIMULACIÓN — NO GENERA COBROS NI PEDIDOS REALES</span><h1 class="mt-3 text-3xl font-black">Simulador de integraciones</h1><div class="card mt-6 p-6"><div class="grid gap-3 text-center sm:grid-cols-5"><div>Evento</div><div>↓ Validación</div><div>↓ Persistencia</div><div>↓ Acción siguiente</div><div>↓ Resultado</div></div></div>@if(session('simulation'))<pre class="mt-5 overflow-auto rounded-2xl bg-[#052f3f] p-5 text-sm text-white">{{session('simulation')}}</pre>@endif<form method="post" action="/admin/simulator" class="card mt-5 grid gap-4 p-6">@csrf<label>Suscripción simulada<select class="field mt-1" name="subscription_id" required>@foreach($subscriptions as $sub)<option value="{{$sub->id}}">{{$sub->provider_subscription_id}} · {{$sub->status}}</option>@endforeach</select></label><label>Evento<select class="field mt-1" name="outcome"><option value="approved">Simular pago aprobado</option><option value="rejected">Simular pago rechazado</option></select></label><label>Ciclo idempotente<input class="field mt-1" name="cycle" value="1"></label><button class="btn btn-primary">Ejecutar escenario</button></form><div class="card mt-5 p-6"><h2 class="font-black">Reglas activas</h2><ul class="mt-3 list-disc pl-5 text-sm"><li>Pago rechazado nunca crea pedido.</li><li>La misma clave de idempotencia no duplica pagos ni pedidos.</li><li>Payloads visibles están sanitizados.</li><li>Todos los eventos llevan <code>is_mock=true</code> y <code>environment=local</code>.</li></ul></div></x-admin-shell>
