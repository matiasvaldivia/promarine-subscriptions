<x-admin-shell><div class="flex flex-wrap justify-between gap-3"><div><h1 class="text-3xl font-black">Informe consolidado</h1><p class="mt-2 text-slate-600">Decisiones, bloqueos y requerimientos derivados.</p></div><div class="flex gap-2"><a class="btn border" href="?format=md">Markdown</a><a class="btn border" href="?format=json">JSON</a><a class="btn border" href="?format=csv">CSV</a></div></div>@foreach(['approved'=>'Decisiones aprobadas','pending'=>'Decisiones pendientes','blocked'=>'Bloqueos','needs_review'=>'Requieren revisión'] as $state=>$heading)<section class="card mt-5 p-6"><h2 class="text-xl font-black">{{$heading}}</h2><div class="mt-3 space-y-3">@forelse($questions->where('status',$state) as $q)<div class="rounded-xl bg-slate-50 p-3"><b>{{$q->question}}</b><p class="text-sm text-slate-600">{{$q->answer?->answer ?: 'Sin respuesta'}}</p><span class="text-xs">{{$q->section->name}} · impacto {{$q->impact}}</span></div>@empty<p class="text-sm text-slate-500">Sin registros.</p>@endforelse</div></section>@endforeach<section class="card mt-5 p-6"><h2 class="text-xl font-black">Impacto técnico</h2><p class="mt-2">Las decisiones críticas pendientes condicionan cobros, reintentos, creación de pedidos, stock, comisiones IGS y textos de consentimiento. Las integraciones reales siguen desconectadas.</p></section></x-admin-shell>
